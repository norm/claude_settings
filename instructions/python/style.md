The python command is `python`.

Use `handle` as the variable name when using "with open()...".

Do not use i, j, etc in enumerate loops, always name variables. Don't create
variables for values used only once.
